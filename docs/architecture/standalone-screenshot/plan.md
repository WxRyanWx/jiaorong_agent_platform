# 实施计划

1. 在 `jiaorong_src/screenshot` 实现进程路径解析、启动、IPC 注册和使用文档。
2. 在宿主启动、快捷键、preload 和快捷键配置处添加最小触点。
3. 增加 Release 下载及目标运行时暂存脚本。
4. 调整 electron-builder、本地构建命令和 GitHub Workflow。
5. 登记宿主触点并执行格式、类型、lint 和脚本验证。
