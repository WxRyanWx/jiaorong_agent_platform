# 实现计划

1. 将设计稿切图迁移到 `brand/icons/skill-detail`，使用英文语义文件名。
2. 在 `SkillDetailPage.vue` 中通过新路径导入预览和源码的默认态、选中态切图。
3. 保持根据 `sourceViewMode` 切换图片源的逻辑和现有显示尺寸。
4. 执行定向格式化、类型检查、i18n 检查和 lint。

## 兼容性与测试

- 保留原按钮、点击事件和 `aria-label`，仅替换视觉资源。
- 静态核对四个状态映射，并运行仓库质量检查。
