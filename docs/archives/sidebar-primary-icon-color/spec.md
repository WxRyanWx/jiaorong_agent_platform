# 侧边栏主图标颜色

## 需求

将侧边栏中的“所有 Agents”“展开/收起侧边栏”和“设置”图标颜色统一为
`#4f698d`。

## 验收标准

- 三个目标图标在亮色和深色主题下均使用 `#4f698d`。
- 图标按钮的选中态、悬停背景和点击行为保持不变。
- 其他 Agent、远程控制和会话操作图标不受影响。

## 约束与非目标

- 仅调整图标前景色，不调整图标、尺寸、布局或交互。
- 宿主组件不得直接写入颜色值；具体颜色必须由
  `src/jiaorong_src/brand/theme.less` 管理。
- 不新增用户可见文案。

## 开放问题

无。
