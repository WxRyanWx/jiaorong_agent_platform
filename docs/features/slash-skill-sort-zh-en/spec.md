# slash-skill-sort-zh-en

## Goal

输入框 `/` 技能面板按名称排序：中文在前（首字拼音）、英文在后（首字母）；逻辑放在私有目录。

## Acceptance

- 同类别内：CJK 首字符技能排在拉丁/数字首字符之前
- 中文组内按 `zh-CN` 拼音序；英文组内按字母序（大小写不敏感）
- 宿主仅接入排序比较器，实现在 `@jiaorong`

## Non-Goals

- 不引入第三方拼音库
- 不改过滤/搜索逻辑
