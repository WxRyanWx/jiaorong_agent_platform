# Plan

1. Strip fake stubs in `api/skills` / `utils` to one-line comment files.
2. Add `pages/SkillListPage/` / `pages/SkillDetailPage/` placeholders (folder per page).
3. Register vue-router routes; sidebar entry after deepchat; i18n + HOST_TOUCHPOINTS.
4. `WindowSideBar`: when `isSkillsRoute`, hide session column and keep shell at icon-rail width (`w-12`).
