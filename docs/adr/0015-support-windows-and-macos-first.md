# Support Windows and macOS first

The first Jiaorong CLI release will formally support macOS on Apple Silicon and Intel and Windows on x64, matching Workbuddian's supported desktop platforms; Linux support is deferred. Every supported target will run the same Protocol Conformance Suite, with explicit coverage for Windows path and shell behavior, cancellation semantics, Unicode, and a bundled runtime that does not require users to install a development toolchain.
