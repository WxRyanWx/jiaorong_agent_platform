# Scope tools to the working directory

Jiaorong CLI will treat its current working directory as the Project Root and default boundary for file and shell tools. Access outside that boundary requires the caller to grant an Additional Directory explicitly, following the established Claude Code, Gemini CLI, and CodeBuddy CLI model; Workbuddian will invoke the CLI with the Obsidian Vault root as its working directory.
