# Discover models through the CLI

Jiaorong CLI will expose a machine-readable model catalog and accept a stable Model ID on each Headless Run. Workbuddian will obtain Jiaorong model choices dynamically through its adapter rather than shipping a hard-coded Jiaorong model allowlist; catalog entries will report display name, default and availability state, supported input types, and optionally context-window size.
