# 安装中禁止跳转详情

## 用户需求

技能市场卡片点击「安装」后处于「安装中」时，不应再通过点击卡片进入详情页。

## 验收标准

- 卡片状态为「安装中」时，点击卡片主体不触发详情跳转
- 「安装」按钮区域本身仍 `@click.stop`，不受影响
- 安装结束后（成功/失败），卡片恢复可点击进入详情

## 约束

- 仅改 `SkillListPage` 相关逻辑/样式，不改详情页与安装工具函数

## 非目标

- 不改安装按钮文案或安装流程本身
- 不引入新的全局状态
