# 计划

- 在 `settingsSidebarAdmin.ts` 增加 `SETTINGS_SPOTLIGHT_HIDDEN_ROUTES`（当前仅 `settings-provider`）和 `isSettingsSpotlightItemHidden`。
- `spotlight.ts` 用该 helper 过滤动作、服务商结果、设置导航结果；不写死路由名。
- 现有 spotlight 测默认当管理员；补非管理员测。
- HOST H127。
