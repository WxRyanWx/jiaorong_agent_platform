# 计划

- 内置技能 YAML `description`：Claude / claude.ai 改成交融AI / 网页。
- `jiaorong-cli/SKILL.md` 示例命令改为 `jiaorong`。
- 打包 CLI 增加 `jiaorong` 启动器（仍调用 `deepchat.mjs`）。
- `electron-builder.yml` extraResources 同步带上 `jiaorong` / `jiaorong.cmd`。
- `agentCommandAccess` 同时认 `jiaorong` 与 `deepchat`。
- HOST 记 bundled CLI 与技能文案。
