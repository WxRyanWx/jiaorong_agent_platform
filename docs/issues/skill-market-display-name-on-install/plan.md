# Plan

1. `ensureSkillMarkdown` 支持 `preferredDisplayName`
2. 标准 YAML 缺 displayName 时，用市场名外科注入（保留其它 frontmatter）
3. `installSkillFromZipUrl({ displayName })` → zip bytes 安装链路
4. 列表 / 详情 / 默认补装传入市场中文名
