# Tasks

- [x] 去掉 merge 中的 alias / displayName 匹配
- [x] merge 仅按唯一 `name` + 单测
- [x] 列表 `remoteInstallMap` enrich / hide 本地 slug（市场卡仍在才藏）
- [x] 修复 ensureSkillMarkdown：市场 preferred 优先于包内 displayName
- [x] 修复 builtin overlay 不覆盖本地已有 displayName
- [x] 修复 applyPreferredDisplayName 损坏 `metadata: {...}` 单行 JSON（超级前端设计安装失败）
