# 详情卸载后列表仍显示「使用」

## 用户需要

详情页删除技能后，返回列表应变回「安装」。

## 目标

在 `@jiaorong/utils` 的 `uninstallSkill` 成功后统一清理交融侧安装记录。

## 验收标准

1. 市场安装 →「使用」→ 详情删除 → 返回列表为「安装」。
2. 详情页、列表页逻辑不为此单独改。

## 约束

清理放在工具层；不改详情页。

## 非目标

列表页防御性清残留 map（已评估非必须）。

## 开放问题

无。
