# Tasks

- [x] Add `docs/issues/corrupt-app-settings-white-screen/{spec,plan,tasks}.md`
- [x] Implement `prepareJsonStoreFile` / quarantine helper under `configPresenter`
- [x] Wire helper + `clearInvalidConfig: true` into `ConfigPresenter` / MCP store creation
- [x] Add unit tests for missing / valid / corrupt JSON
- [ ] Run format / lint / targeted tests
