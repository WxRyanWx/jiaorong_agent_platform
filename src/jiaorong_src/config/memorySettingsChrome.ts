/** 记忆设置页外壳：藏入口不删代码，以后改 true 即可放开。 */

/** 工具栏「配置 / 开启记忆」按钮。必须用 v-if，不能打在 DcButton 的 v-show 上。 */
export const SHOW_MEMORY_CONFIGURE_BUTTON = false

/** 状态栏「嵌入模型 / 未配置嵌入模型」文案 */
export const SHOW_MEMORY_STATUS_EMBEDDING_LABEL = false

/** 配置面板里原来的长期记忆开关（已提升到工具栏） */
export const SHOW_MEMORY_CONFIG_PANEL_ENABLE_SWITCH = false

/** 「诊断」Tab */
export const SHOW_MEMORY_DIAGNOSTICS_TAB = false
